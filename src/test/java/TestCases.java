
import com.kubitssu.Operations;
import com.kubitssu.executor.CalculatorExecutor;
import com.kubitssu.executor.Executor;
import com.kubitssu.reader.CalculatorReader;
import com.kubitssu.reader.Reader;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;
/*
public class TestCases {
    private String Path = "D:\\Projects\\Calculator\\src\\test\\resources";
    Reader readTest = new CalculatorReader();
    Executor execTest = new CalculatorExecutor();

    @Test
    void CountTestOne() {
        execTest.executor(readTest.readFile(Path + "Test1.txt"));
        assertEquals(36, execTest.getOutputValue());
    }

    @Test
    void CountTestTwo() {
        execTest.executor(readTest.readFile(Path + "Test2.txt"));
        assertEquals(32, execTest.getOutputValue());
    }

    @Test
    void readTest() {
        assertEquals(10, readTest.readFile(Path + "Test2.txt").get(Operations.APPLY));
    }

}*/
